public enum ComponentType
{
    Sprite,
    PlayerInput,
    Animation,
    Collision
}

public enum Input
{
    Left,
    Right,
    Up,
    Down,
    None
}

public enum Direction
{
    Left,
    Right, 
    Up,
    Down
}

public enum State
{
    Standing,
    Walking,
}